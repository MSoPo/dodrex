import { TipoNominaPipe } from './tipo-nomina.pipe';

describe('TipoNominaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoNominaPipe();
    expect(pipe).toBeTruthy();
  });
});
